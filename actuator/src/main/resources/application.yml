 ###通过下面的配置(management..)启用所有的监控端点，默认情况下，这些端点是禁用的；
 management:
   endpoints:
     web:
       exposure:
         include: "*"


 spring:
   profiles:
     active: prod
   datasource:
     driver-class-name: com.mysql.jdbc.Driver
     url: jdbc:mysql://192.168.40.128:3306/test01
     username: root
     password: 654321Bai.

 info:
      itmayiedu: yushengjun
      addres: www.itmayiedu.com

